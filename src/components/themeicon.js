import React from 'react'
import { foreground, lightForeground } from "../theme.js"

export const Icon = ({
    fill, width, height,
}) => {
    <svg width={width} height={height} xmlns="http://www.w3.org/2000/svg" id="Layer_1" data-name="Svg Icon" viewBox="0 0 24 24">
    <path style={ { fill } } d={name}/>
    </svg>
}
